node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# variáveis de ambiente; só o exemplo é versionado
.env*
!.env.example

# credenciais da service account do Google Earth Engine
gen-lang-client-*.json
service-account*.json

# caches de bytecode do Python (scripts/)
__pycache__/

# saída do scripts/build-sif.py; baixada do Earthdata, não versionada
data/sif-esdr/

# documentos de referência mantidos só localmente
*.docx

# Subagent-driven development workspace (ledger, briefs, review packages)
.superpowers/
