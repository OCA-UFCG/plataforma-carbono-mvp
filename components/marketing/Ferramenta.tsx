import styles from "./Ferramenta.module.css";

// Filled in by Task 7. The wrapper and its id ship now so the nav anchors resolve.
export default function Ferramenta() {
  return <section id="ferramenta" className={styles.ferramenta} aria-label="Ferramenta" />;
}
