import styles from "./SiteFooter.module.css";

// Filled in by Task 9. The wrapper ships now so the page shell can be composed
// against a stable component tree.
export default function SiteFooter() {
  return <footer className={styles.siteFooter} aria-label="Rodapé" />;
}
