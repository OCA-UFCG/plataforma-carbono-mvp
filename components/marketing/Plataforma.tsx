import styles from "./Plataforma.module.css";

// Filled in by Task 6. The wrapper and its id ship now so the nav anchors resolve.
export default function Plataforma() {
  return <section id="plataforma" className={styles.plataforma} aria-label="Plataforma" />;
}
