import type { Metadata } from "next";

export const metadata: Metadata = { title: "Entenda essa relação" };

// "Entenda essa relação", Figma frame 18988:8769. Only the shared frame from
// sobre/layout.tsx renders until issue #51 fills in the content.
export default function SobreCarbonoComunidadesPage() {
  return null;
}
