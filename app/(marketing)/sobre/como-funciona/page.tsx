import type { Metadata } from "next";

export const metadata: Metadata = { title: "Como funciona" };

// "Como funciona", Figma frame 18988:8943. Only the shared frame from
// sobre/layout.tsx renders until issue #50 fills in the content.
export default function SobreComoFuncionaPage() {
  return null;
}
